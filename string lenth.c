#include<stdio.h>
void main()
{
int len;
char str1[25]="Hi,How are you baby";
char str2[40]="you have very beautiful eyes";
len= strlen(str1);
printf("strlen(str1) : %d\n",len);
len = strlen(str2);
printf("strlen(str1) : %d\n",len);
}
